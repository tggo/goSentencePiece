name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: ['1.22', '1.25', 'stable']

    steps:
      - uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}

      - name: Build
        run: go build ./...

      - name: Test with coverage
        run: go test -v -count=1 -coverprofile=coverage.out -coverpkg=github.com/tggo/goSentencePiece ./...

      - name: Show coverage
        run: go tool cover -func=coverage.out | tail -1

      - name: Vet
        run: go vet ./...

      - name: Install golangci-lint
        if: matrix.go-version == 'stable'
        run: |
          curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh | sh -s -- -b $(go env GOPATH)/bin
          echo "$(go env GOPATH)/bin" >> $GITHUB_PATH

      - name: Lint
        if: matrix.go-version == 'stable'
        run: golangci-lint run ./...

      - name: Verify go generate produces no diff
        if: matrix.go-version == 'stable'
        run: |
          go generate ./...
          git diff --exit-code || (echo "go generate produced changes; please run 'go generate ./...' and commit the result" && exit 1)

      - name: Upload coverage
        if: matrix.go-version == 'stable'
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: coverage.out
