version: "2"

run:
  timeout: 5m

formatters:
  enable:
    - gofmt

linters:
  enable:
    - govet
    - staticcheck
    - ineffassign
    - misspell
    - gocyclo
    - errcheck
    - gosimple
    - unused

  settings:
    gocyclo:
      min-complexity: 15

issues:
  exclude-dirs:
    - proto
